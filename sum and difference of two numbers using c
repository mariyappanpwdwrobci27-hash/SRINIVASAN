#include <stdio.h>

int main() {
    int i1, i2;
    float f1, f2;

    // Read two integers from the first line
    scanf("%d %d", &i1, &i2);
    
    // Read two floats from the second line
    scanf("%f %f", &f1, &f2);

    // Print integer sum and difference
    printf("%d %d\n", i1 + i2, i1 - i2);

    // Print float sum and difference rounded to one decimal place
    // %.1f ensures the output is scaled to one decimal point
    printf("%.1f %.1f\n", f1 + f2, f1 - f2);

    return 0;
}
