#include <stdio.h>

int main() {
    int n, sum = 0;
    scanf("%d", &n);
    
    // Process all 5 digits
    while (n > 0) {
        sum += (n % 10); // Add the last digit to sum
        n = n / 10;      // Remove the last digit
    }
    
    printf("%d\n", sum);
    return 0;
}
